apiVersion: batch/v1
kind: Job
metadata:
  name: python-job
spec:
  template:
    spec:
      containers:
        - name: python-job
          image: python:latest
          command: [ "python", "-c" ]
          args: [ "print('Hello from the Kubernetes job')" ]
      restartPolicy: Never
  backoffLimit: 4
  